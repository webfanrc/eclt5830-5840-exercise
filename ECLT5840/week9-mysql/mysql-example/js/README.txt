db.js -- Connect to the database and export a "connection pool" object.

model.js -- 
  Contains examples that use mysql APIs to perform 
  CRUD operations

test.js -- To test the helper functions in model.js.
(Can be executed as "node test.js")

